[phases.setup]
nixPkgs = ["python312", "gcc", "gdal"]

[phases.install]
cmds = ["/opt/venv/bin/python -m pip install --upgrade pip", "/opt/venv/bin/python -m pip install -r requirements.txt"]

[phases.build]
cmds = ["chmod +x build.sh && chmod +x start.sh"]

[start]
cmd = "bash start.sh"
